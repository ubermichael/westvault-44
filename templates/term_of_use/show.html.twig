{% extends 'base.html.twig' %}

{% block title %}Term Of Use Details {% endblock %}

 {% block pageheader %}
     <h1>Term Of Use Details</h1>
 {% endblock %}

{% block body %}

    {% if is_granted('ROLE_CONTENT_ADMIN') %}
        <div class='btn-toolbar pull-right'>
            <div class='btn-group'>
                <a href="{{ path('term_of_use_edit', {'id': term_of_use.id }) }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-edit"></span> Edit </a>
            </div>
            <div class='btn-group'>
                <form class='delete-form' method="post" action="{{ path('term_of_use_delete', {'id': term_of_use.id }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token("delete" ~ term_of_use.id) }}">
                    <button class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Delete</button>
                </form>
            </div>
        </div>
    {% endif %}

    {% embed 'term_of_use/partial/detail.html.twig' %}
    {% endembed %}

    {% embed 'term_of_use/partial/history.html.twig' %}
    {% endembed %}

{% endblock %}
