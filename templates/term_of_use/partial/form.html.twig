{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <input type="submit" value="Save" class="btn btn-primary" />
        {% if term_of_use.id is null %}
            <a href="{{ path('term_of_use_index') }}" class="btn btn-warning"> Cancel </a>
        {% else %}
            <a href="{{ path('term_of_use_show', { 'id': term_of_use.id }) }}" class="btn btn-warning"> Cancel </a>
        {% endif %}
    </div>
</div>
{{ form_end(form) }}

